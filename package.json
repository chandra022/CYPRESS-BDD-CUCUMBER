{
  "name": "cypress-bdd-cucumber_sc",
  "version": "1.0.0",
  "description": "Reference from SC",
  "main": "index.js",
  "scripts": {
    "cypress:chrome": "cypress run --browser chrome --headed",
    "cypress:edge": "cypress run --browser chrome",
    "cypress:api": "cypress run --spec"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^12.14.0",
    "cypress-mochawesome-reporter": "^3.5.1",
    "esbuild": "^0.18.4",
    "moment": "^2.29.4"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/specs/**/*.feature.{js,ts}"
    ]
  }
}
